<iscontent type="application/json" charset="UTF-8" compact="true"/>
<isscript>
    var co = pdict.CatalogReducerInfo;
    var obj = {};
    if (!empty(co)) {
        obj = {
            running: co.custom.running,
            progress: co.custom.progress
        };
    }
</isscript>
<isprint value="${JSON.stringify(obj)}" encoding="off"/>
